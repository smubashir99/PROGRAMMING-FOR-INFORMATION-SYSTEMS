# DOM Task

This is a simple exercise for Programming for Information Systems.  

- There is a textbox on the page.  
- Whatever the user types is shown below in real-time.  
- Uses basic JavaScript DOM (`onkeyup` event) to update the div.  

Open `index.html` in a browser to try it.
